# Bridging API to the Ethereum Network

## Setup

Install [Glide](http://glide.sh/) for package manager:

```sh
$ brew install glide
```

Install protobuf:

```sh
$ brew install protobuf
```

Then execute `glide install`:

```sh
$ glide install
```
